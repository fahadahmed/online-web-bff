/**
 * This file was auto-generated by openapi-typescript.
 * Do not make direct changes to the file.
 */

export interface paths {
  '/v2/user/access/{user}': {
    get: operations['getFullAccessData'];
  };
  '/v2/user/access/{user}/account': {
    get: operations['getFullAccountAccessData'];
    post: operations['addAccountAccess'];
  };
  '/v2/user/access/{user}/line': {
    get: operations['getFullLineAccessData'];
    post: operations['addLineAccess'];
  };
  '/v2/user/access/{user}/identity': {
    get: operations['getFullIdentityAccessData'];
  };
  '/v2/user/access/{user}/line/{lineNumber}': {
    delete: operations['removeLineAccess'];
  };
  '/v2/user/access/{user}/line/{lineNumber}/alias': {
    patch: operations['editLineAccess'];
  };
  '/v2/user/access/{user}/account/{accountNumber}/alias': {
    patch: operations['editAccountAccess'];
  };
  '/v2/user/access/{user}/account/{accountNumber}': {
    delete: operations['removeAccountAccess'];
  };
  '/v2/user/access/{user}/line/{lineNumber}/{right}': {
    get: operations['getLineAccessData'];
  };
  '/v2/user/access/{user}/account/{accountNumber}/{right}': {
    get: operations['getAccountAccessData'];
  };
  '/v2/user/access/{user}/identity/{right}': {
    get: operations['getIdentityAccessData'];
  };
  '/v2/user/access/account/{accountNumber}': {
    /** Note that the requesting user must be the owner or manager of the account */
    get: operations['getAccessForAccount'];
  };
  '/v2/user/access/line/{lineNumber}': {
    /** Note that the requesting user must be the owner or manager of the line */
    get: operations['getAccessForLine'];
  };
}

export interface definitions {
  AccessResponse: definitions['Response'] & {
    lines: definitions['SingleAccessRecord'][];
    accounts: definitions['SingleAccessRecord'][];
    identities: definitions['SingleAccessRecord'][];
  };
  AccessPermissionResponse: definitions['Response'] & {
    access: definitions['SinglePermissionRecord'][];
  };
  SingleAccessRecord: {
    /** The line of the access record (only present in line-typed records) */
    lineNumber?: string;
    /** The customer number of the access record (present in account & line-typed records) */
    customerNumber?: string;
    /** The account of the access record (present in account & line-typed records; PP prefixed account numbers should only show up in line-typed records) */
    accountNumber?: string;
    /** Revoked is only for accounts, and will only show if such accounts are requested.  NotFound is only for accounts & lines, ditto. */
    status: 'Active' | 'Revoked' | 'NotFound';
    /** The type of the access record */
    type: 'Line' | 'Account' | 'Identity';
    /** The type of the line (if this is a line level access record), or of all the lines under the account (if this is an account level access record).  Strictly speaking, this is an account-level attribute, but we mostly use it at line level, so I have phrased it that way. */
    balanceManagement?: 'PREPAID' | 'POSTPAID';
    /** The role of the access record */
    role: string;
    /** The user-friendly alias for the access record */
    aliasName?: string;
    /** The line numbers associated with this account (will only be present in non-revoked account-typed records).  If this is returned via a line-level access request, this will contain at most the requested line number. */
    lineNumbers?: string[];
    /** The list of rights (aka eligibilities) that the role grants (will only be present in account & line records). */
    rights?: string[];
  };
  AccessRecord: definitions['Response'] & {
    access: definitions['SingleAccessRecord'];
  };
  SinglePermissionRecord: {
    /** The Spark Identity which has access. */
    uuid: string;
    /** Indicates whether the access is given (through added or granted access) or inherited (via having high level access to the account).  Note that the current implementation will be unable to populate this accurately. */
    type: 'GIVEN' | 'INHERITED';
    /** The email address of the Spark Identity. */
    email: string;
    /** The first name the user has setup for their Spark Identity. */
    firstName?: string;
    /** The last name the user has setup for their Spark Identity. */
    lastName?: string;
    /** The business name the user has setup for their Spark Identity. */
    businessName?: string;
    /** The role of the access the Spark Identity has.  This will be unpopulated when the access is still pending. */
    role?: string;
    /** If access has been requested but not approved for this spark identity */
    isPending?: boolean;
    /** If the access has been revoked due to the Siebel password being changed */
    isRevoked?: boolean;
  };
  AccountAccessRequest: {
    /** The account number of the account which we intend to add access to */
    accountNumber: string;
    authorisation: definitions['AccountAuthorisation'];
    /** The user-friendly alias to be assigned to the access record */
    aliasName?: string;
    /** The proposed role of the access record; in some cases, a different role may be assigned */
    role: string;
  };
  /** A bunch of properties needed to authorise the add access to this account.  Note that if the account already has an owner or manager, none of these are required.  If only the line number is provided, the API returns a 300 error response with a code indicating which of password, business name, or first/last name is also needed. */
  AccountAuthorisation: {
    /** A line number which belongs to the account; if authorisation is needed, this is required. */
    lineNumber?: string;
    /** The first name of the customer that owns the account; if first name is provided, so too should be lastName */
    firstName?: string;
    /** The last name of the customer that owns the account */
    lastName?: string;
    /** The business name of the customer that owns the account; one of business name or first name (but not both) should be provided */
    businessName?: string;
    /** The password attached to the customer that owns the account.  If authorisation is needed, either this, or a name is required. */
    password?: string;
  };
  LineAccessRequest: {
    /** The line number of the line which we intend to add access to */
    lineNumber: string;
    authorisation: definitions['LineAuthorisation'];
    /** The user-friendly alias to be assigned to the access record */
    aliasName?: string;
    /** The proposed role of the access record; in some cases, a different role may be assigned */
    role: string;
  };
  /** A bunch of properties needed to authorise the add access to this line. */
  LineAuthorisation: {
    /** A 4 digit SMS-sent code to authorise this access addition.  If it is not provided, one will be sent, and a 300 response returned */
    authCode?: string;
    /** The type of the line.  Strictly speaking, this is an account-level attribute, but we mostly use it at line level, so I have phrased it that way. */
    balanceManagement: 'PREPAID' | 'POSTPAID';
  };
  EditAccessRequest: {
    /** The user-friendly alias to be assigned to the access record */
    aliasName: string;
  };
  Response: {
    /** List of Messages */
    messages: definitions['Message'][];
  };
  Message: {
    message: string;
    code: number;
  };
}

export interface operations {
  getFullAccessData: {
    parameters: {
      path: {
        /** The user whose access is being accessed.  Currently restricted to 'me' */
        user: string;
      };
      query: {
        /** If true, revoked account access records will be returned */
        includeRevokedAccess?: boolean;
        /** If true, account (or line) access records for which the account (or line, respectively) cannot be found will be returned */
        includeNotFoundRecords?: boolean;
      };
    };
    responses: {
      /** OK */
      200: {
        schema: definitions['AccessResponse'];
      };
      /** The provided token is invalid, or access is otherwise denied. */
      401: {
        schema: definitions['Response'];
      };
      /** The server failed to process the request properly. */
      500: {
        schema: definitions['Response'];
      };
    };
  };
  getFullAccountAccessData: {
    parameters: {
      path: {
        /** The user whose account-level access is being accessed.  Currently restricted to 'me' */
        user: string;
      };
      query: {
        /** If true, revoked account access records will be returned */
        includeRevokedAccess?: boolean;
        /** If true, account access records for which the account cannot be found will be returned */
        includeNotFoundRecords?: boolean;
      };
    };
    responses: {
      /** OK */
      200: {
        schema: definitions['AccessResponse'];
      };
      /** The provided token is invalid, or access is otherwise denied. */
      401: {
        schema: definitions['Response'];
      };
      /** The server failed to process the request properly. */
      500: {
        schema: definitions['Response'];
      };
    };
  };
  addAccountAccess: {
    parameters: {
      path: {
        /** The user to whom we are adding an account.  Currently restricted to 'me' */
        user: string;
      };
      body: {
        /** access request */
        AccountAccessRequest?: definitions['AccountAccessRequest'];
      };
    };
    responses: {
      /** OK */
      200: {
        schema: definitions['Response'];
      };
      /** Insufficient authorisation data was provided */
      300: {
        schema: definitions['Response'];
      };
      /** The provided data was invalid */
      400: {
        schema: definitions['Response'];
      };
      /** The provided token is invalid, or access is otherwise denied. */
      401: {
        schema: definitions['Response'];
      };
      /** The server failed to process the request properly. */
      500: {
        schema: definitions['Response'];
      };
    };
  };
  getFullLineAccessData: {
    parameters: {
      path: {
        /** The user whose line-level access is being accessed.  Currently restricted to 'me' */
        user: string;
      };
      query: {
        /** If true, line access records for which the line cannot be found will be returned */
        includeNotFoundRecords?: boolean;
      };
    };
    responses: {
      /** OK */
      200: {
        schema: definitions['AccessResponse'];
      };
      /** The provided token is invalid, or access is otherwise denied. */
      401: {
        schema: definitions['Response'];
      };
      /** The server failed to process the request properly. */
      500: {
        schema: definitions['Response'];
      };
    };
  };
  addLineAccess: {
    parameters: {
      path: {
        /** The user to whom we are adding a line.  Currently restricted to 'me' */
        user: string;
      };
      body: {
        /** access request */
        LineAccessRequest?: definitions['LineAccessRequest'];
      };
    };
    responses: {
      /** OK */
      200: {
        schema: definitions['Response'];
      };
      /** Insufficient authorisation data was provided */
      300: {
        schema: definitions['Response'];
      };
      /** The provided data was invalid */
      400: {
        schema: definitions['Response'];
      };
      /** The provided token is invalid, or access is otherwise denied. */
      401: {
        schema: definitions['Response'];
      };
      /** The server failed to process the request properly. */
      500: {
        schema: definitions['Response'];
      };
    };
  };
  getFullIdentityAccessData: {
    parameters: {
      path: {
        /** The user whose identity-level access is being accessed.  Currently restricted to 'me' */
        user: string;
      };
    };
    responses: {
      /** OK */
      200: {
        schema: definitions['AccessResponse'];
      };
      /** The provided token is invalid, or access is otherwise denied. */
      401: {
        schema: definitions['Response'];
      };
      /** The server failed to process the request properly. */
      500: {
        schema: definitions['Response'];
      };
    };
  };
  removeLineAccess: {
    parameters: {
      path: {
        /** The user whose access is being removed.  This is either a Spark Id, or 'me' - corresponding to the current logged in user. */
        user: string;
        /** The line number, with leading zero */
        lineNumber: string;
      };
    };
    responses: {
      /** OK */
      200: {
        schema: definitions['Response'];
      };
      /** The provided token is invalid, or the user does not have (appropriate) access to the line. */
      401: {
        schema: definitions['Response'];
      };
      /** The server failed to process the request properly. */
      500: {
        schema: definitions['Response'];
      };
    };
  };
  editLineAccess: {
    parameters: {
      path: {
        /** The user whose access is being altered.  This is either a Spark Id, or 'me' - corresponding to the current logged in user. */
        user: string;
        /** The line number, with leading zero */
        lineNumber: string;
      };
      body: {
        /** edit access request */
        EditAccessRequest?: definitions['EditAccessRequest'];
      };
    };
    responses: {
      /** OK */
      200: {
        schema: definitions['Response'];
      };
      /** The provided token is invalid, or the user does not have (appropriate) access to the line. */
      401: {
        schema: definitions['Response'];
      };
      /** The server failed to process the request properly. */
      500: {
        schema: definitions['Response'];
      };
    };
  };
  editAccountAccess: {
    parameters: {
      path: {
        /** The user whose access is being altered.  This is either a Spark Id, or 'me' - corresponding to the current logged in user. */
        user: string;
        /** The account number */
        accountNumber: string;
      };
      body: {
        /** edit access request */
        EditAccessRequest?: definitions['EditAccessRequest'];
      };
    };
    responses: {
      /** OK */
      200: {
        schema: definitions['Response'];
      };
      /** The provided token is invalid, or the user does not have (appropriate) access to the account. */
      401: {
        schema: definitions['Response'];
      };
      /** The server failed to process the request properly. */
      500: {
        schema: definitions['Response'];
      };
    };
  };
  removeAccountAccess: {
    parameters: {
      path: {
        /** The user whose access is being removed.  This is either a Spark Id, or 'me' - corresponding to the current logged in user. */
        user: string;
        /** The account number */
        accountNumber: string;
      };
    };
    responses: {
      /** OK */
      200: {
        schema: definitions['Response'];
      };
      /** The provided token is invalid, or the user does not have (appropriate) access to the line. */
      401: {
        schema: definitions['Response'];
      };
      /** The server failed to process the request properly. */
      500: {
        schema: definitions['Response'];
      };
    };
  };
  getLineAccessData: {
    parameters: {
      path: {
        /** The user whose access is being accessed.  Currently restricted to 'me' */
        user: string;
        /** The line number, with leading zero */
        lineNumber: string;
        /** The specific right that the user must have for this line */
        right: string;
      };
    };
    responses: {
      /** OK */
      200: {
        schema: definitions['AccessRecord'];
      };
      /** The provided token is invalid, or the user does not have (appropriate) access to the line. */
      401: {
        schema: definitions['Response'];
      };
      /** The server failed to process the request properly. */
      500: {
        schema: definitions['Response'];
      };
    };
  };
  getAccountAccessData: {
    parameters: {
      path: {
        /** The user whose access is being accessed.  Currently restricted to 'me' */
        user: string;
        /** The account number */
        accountNumber: string;
        /** The specific right that the user must have for this account */
        right: string;
      };
    };
    responses: {
      /** OK */
      200: {
        schema: definitions['AccessRecord'];
      };
      /** The provided token is invalid, or the user does not have (appropriate) access to the account. */
      401: {
        schema: definitions['Response'];
      };
      /** The server failed to process the request properly. */
      500: {
        schema: definitions['Response'];
      };
    };
  };
  getIdentityAccessData: {
    parameters: {
      path: {
        /** The user whose access is being accessed.  Currently restricted to 'me' */
        user: string;
        /** The specific right that the user must have for this identity */
        right: string;
      };
    };
    responses: {
      /** OK */
      200: {
        schema: definitions['AccessRecord'];
      };
      /** The provided token is invalid, or the identity does not have the requested right. */
      401: {
        schema: definitions['Response'];
      };
      /** The server failed to process the request properly. */
      500: {
        schema: definitions['Response'];
      };
    };
  };
  /** Note that the requesting user must be the owner or manager of the account */
  getAccessForAccount: {
    parameters: {
      path: {
        /** The account number whose access is being fetched. */
        accountNumber: string;
      };
    };
    responses: {
      /** OK */
      200: {
        schema: definitions['AccessPermissionResponse'];
      };
      /** The provided token is invalid. */
      401: {
        schema: definitions['Response'];
      };
      /** The user doesn't have (sufficient) access to the account.  This probably includes cases where the account doesn't exist. */
      403: {
        schema: definitions['Response'];
      };
      /** The server failed to process the request properly. */
      500: {
        schema: definitions['Response'];
      };
    };
  };
  /** Note that the requesting user must be the owner or manager of the line */
  getAccessForLine: {
    parameters: {
      path: {
        /** The line number whose access is being fetched. */
        lineNumber: string;
      };
    };
    responses: {
      /** OK */
      200: {
        schema: definitions['AccessPermissionResponse'];
      };
      /** The provided token is invalid. */
      401: {
        schema: definitions['Response'];
      };
      /** The user doesn't have (sufficient) access to the line.  This probably includes cases where the line doesn't exist. */
      403: {
        schema: definitions['Response'];
      };
      /** The server failed to process the request properly. */
      500: {
        schema: definitions['Response'];
      };
    };
  };
}
